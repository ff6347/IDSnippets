// written by fabiantheblind// http://www.the-moron.net// convert colors still a bit buggy (RGB 2 CMYK works fine CMYK 2 RGB is buggy)// opend a thread at HDS http://tinyurl.com/625sg52/*for(var i = app.activeDocument.colors.length; i >=0 ; i--   ){        try{        app.activeDocument.colors.item(i).space = ColorSpace.CMYK;        }catch(e){        $.writeln(e);        }      }    */for(var i =0; i < app.activeDocument.colors.length; i++   ){        try{        app.activeDocument.colors.item(i).space = ColorSpace.CMYK;        }catch(e){        $.writeln(e);        }      }    