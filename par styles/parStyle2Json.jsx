main();function main(){    var ref = app.activeDocument.paragraphStyles.item(1).properties;var obj = app.activeDocument.paragraphStyles.item("h1").properties;alert(obj.name);var arr = new Array();for(var i in obj){    //~     if(obj[i] !=  ref[i])     arr.push("\"" + i +"\":" + obj[i]);         }var newLocation = Folder.selectDialog("Select a output folder...");    var timestamp = Number(new Date());	var fn =  "file "+ timestamp;	var tf = newLocation.fsName;	var txtFile =  new File( tf+"/" + fn + ".json" );     writeArray(txtFile,arr);//~ alert("\n"+arr);}function writeArray (txtFile , arr ){    var out;   if( txtFile!='' )     {         //Open the file for writing.         out = txtFile.open( 'e', undefined, undefined );      txtFile.encoding = "UTF-8";      txtFile.lineFeed = "Unix"; //convert to UNIX linefeed   }     if( out != false )     {          for(var i in arr){      txtFile.writeln(arr[i] );     }      txtFile.close();        // txtFile.execute();     }}