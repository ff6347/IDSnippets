// paragraphs to textframe// written by fabiantheblind// http://www.the-moron.netvar sel = app.selection[0];    var p = app.activeDocument.pages.add();for(var i = 0;i < sel.paragraphs.length;i++){    t = p.textFrames.add();    t.geometricBounds = [0, 0,20, 200 ];    t.contents = sel.paragraphs.item(i).contents;    t.paragraphs.item(0).appliedParagraphStyle = sel.paragraphs.item(i).appliedParagraphStyle.name;     }